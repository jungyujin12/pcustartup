# AI 실행기 사용 안내

## 최초 1회

1. Firebase Console → 프로젝트 설정 → 서비스 계정으로 이동합니다.
2. 새 비공개 키를 생성해 JSON 파일을 안전한 폴더에 저장합니다.
3. `.env.example`을 `.env`로 복사하고 Gmail 앱 비밀번호를 입력합니다.
4. `AI 실행기 V8.exe`를 실행합니다.
5. `JSON 선택`에서 서비스 계정 파일을 선택합니다.
6. `AI 작업 시작`을 누릅니다.

## 평상시

Ollama가 설치된 PC에서 `AI 실행기 V8.exe`만 실행하면 됩니다. 실행기 상단에
`Ollama 정상`, `AI 작업 실행 중`이 표시되면 보고서 분석과 메일 대기열이
자동으로 처리됩니다.

## 주의

- 서비스 계정 JSON과 `.env`는 GitHub, Netlify, 메신저에 올리지 마세요.
- 서비스 계정 JSON은 `final_deploy` 폴더 안에 두지 마세요.
- 실행기를 끄면 파일 업로드는 가능하지만 AI 분석과 메일은 대기 상태로 남습니다.
- 실패한 작업은 최대 3회 자동 재시도합니다.

# 무료 웹사이트 투트랙 생성

AI 실행기가 실행 중이면 웹사이트의 `웹사이트·포트폴리오 제작` 화면에서 등록된
`website_jobs` 작업도 자동으로 처리합니다. 별도의 Claude, Gemini, Grok API 키는
필요하지 않으며 `OLLAMA_TEXT_MODEL`에 설정된 로컬 모델을 사용합니다.

- 창업 목적 웹사이트: 문제, 해결책, 제품·서비스, 활동과 검증 결과 중심
- 취업용 포트폴리오: 희망 직무, 역량, 프로젝트, 경험과 경력 중심

처리 흐름:

1. 사용자가 텍스트 또는 PDF와 생성 옵션을 입력합니다.
2. Firebase `website_jobs`에 작업이 등록됩니다.
3. AI 실행기가 Ollama로 PDF·사업계획서 원문을 사실 기반 항목으로 먼저 구조화합니다.
4. AI가 콘텐츠 밀도·주제·근거·서사를 분석해 프로젝트 전용 화면 설계도를 만듭니다.
5. 4060 고품질형은 설계도를 바탕으로 동적 HTML을 만들고 자동 품질검사를 수행합니다. 기준 미달 결과는 공개하지 않고 검증된 렌더러로 자동 교체합니다.
6. 1650 절전형은 업무용 PC의 자원을 보호하기 위해 AI 기획 결과와 검증된 렌더러를 결합해 순차 처리합니다.
7. 결과는 `generated-site.html?id=페이지주소`에서 확인하고 상단의 강조된 `HTML 다운로드` 버튼으로 내려받을 수 있습니다.

## 컴퓨터 사용 방식

- `회사 PC 절전형 · GTX 1650`: 낮은 프로세스 우선순위, 동시 작업 1개, 작업 사이 8초 휴식, 8K 문맥을 사용합니다.
- `개인 PC 고품질형 · RTX 4060`: 24K 문맥과 최대 2회 보정을 사용합니다. AI 자유 생성물이 기준에 못 미치면 검증 렌더러로 대체합니다.
- 두 실행 파일은 기능이 같으며 파일명에 따라 첫 선택값만 다릅니다. 실행기 화면에서 언제든 프로필을 바꿀 수 있습니다.
- 웹사이트 내용 분석 모델은 무료 로컬 모델 `qwen2.5-coder:7b`입니다. 이미지 생성 서비스나 유료 API는 사용하지 않습니다.
- HTML 본문은 입력 사실을 기준으로 조립하고, 최종 화면은 V8.1 품질 합성기가 에디토리얼·근거 중심·휴먼 스토리 문법으로 구성합니다. 로컬 AI가 흔한 그라디언트·카드 CSS를 임의 생성하지 못하게 차단했습니다.

사용자가 직접 입력한 값은 AI가 덮어쓰지 않습니다. 비어 있거나 `본문 참고`처럼 내용이 없는 항목만 PDF 원문 분석으로 보강하며, 원문 전문은 공개 웹사이트에 그대로 노출하지 않습니다.

AI 실행기가 꺼져 있으면 작업은 대기 상태로 남습니다. 실행기를 켜면 순서대로
처리됩니다. 생성이 완료되면 원본 입력 내용은 작업 문서에서 자동 삭제됩니다.
# 승인형 디자인 라이브러리

관리자 화면의 `디자인 라이브러리`에서 창업·취업 종류와 생성 개수를 지정하면 AI 실행기가 유휴 시간에 실제 검수용 HTML 후보를 만듭니다. 후보는 기본적으로 `검토 대기` 상태이며, 관리자가 실제 HTML을 PC와 모바일에서 확인하고 승인해야 학생 제작 추천에 사용됩니다. 승인된 신규 디자인은 다음 학생 신청부터 자동으로 후보에 포함되고, 사용 횟수가 적으면서 자료 구조에 적합한 디자인이 우선 추천됩니다.

업무용 GTX 1650에서는 한 번에 1~3개를 권장합니다. 많은 수량은 퇴근 후 요청하고, 생성 중 PC를 끄면 처리 중이던 작업은 실패로 기록될 수 있으므로 작업 완료 후 종료하세요.
