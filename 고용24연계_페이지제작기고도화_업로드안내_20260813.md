# 적용 방법

## GitHub에 덮어쓸 파일

아래 파일은 저장소의 `final_deploy` 폴더에 같은 이름으로 덮어쓴다.

1. `index.html`
2. `career-test.html`
3. `website-builder.html`
4. `career-admin.html`

저장소 루트에는 운영 참고용 `WORK24_INTEGRATION_NOTICE.md`를 올릴 수 있다. 이 문서는 배포 동작에 필수는 아니다.

`CAREER_TEST_DESIGN.md`가 GitHub의 `final_deploy`에 남아 있으면 삭제한다. 자체 검사 설계 문서가 공개 배포물에 남지 않게 하기 위한 조치다.

## 관리자 PC에서 교체할 파일

`AI 실행기.exe`는 GitHub나 Netlify에 올리지 않는다. 기존 실행기를 종료하고 관리자 PC의 실행 파일만 새 버전으로 교체한다. Firebase 서비스 계정 JSON은 기존 파일을 그대로 선택하며 ZIP이나 GitHub에 넣지 않는다.

## 배포 후 확인

1. 첫 화면의 `고용24 직업심리검사 안내`를 연다.
2. `결과 PDF로 제작하기`를 누르면 취업 제작기와 `공식 검사 결과 참고`가 선택되는지 확인한다.
3. 고용24 등에서 직접 받은 결과 PDF와 이력서 PDF를 선택한다.
4. AI 실행기를 켜고 테스트 페이지 하나를 생성한다.
5. 결과 페이지의 HTML 다운로드와 모바일 표시를 확인한다.

고용24 문항, 채점 방식, 결과 해설, 로고 및 화면을 PCU 사이트에 복사하지 않는다.
