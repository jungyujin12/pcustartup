# PCU 창업지원 시스템 최종 점검 보고서

점검일: 2026-08-12  
대상: `final_deploy`, Firebase 규칙, Netlify 설정, 로컬 Ollama AI 실행기

## 1. 종합 판정

- 핵심 운영 파이프라인은 연결되어 있다.
- 공개·학생 화면 15종의 링크, 스크립트 문법, 데스크톱 레이아웃을 확인했다.
- 모바일 390×844 기준으로 전체 화면을 검사했고 상담 신청 및 진로 관리자 화면의 가로 넘침을 수정했다.
- 동아리·학생·연도 원장은 `clubs/{immutableClubId}`, `students/{studentId}`, `memberships/{year_studentId}` 구조를 사용한다.
- 접근코드 변경은 원천 동아리 문서 ID와 파일 경로를 바꾸지 않고 `accessCode`와 해시 색인만 바꾸므로 기존 팀원·문서 이력이 유지된다.
- 관리자 무결성 진단은 중복 동아리, 코드 해시/이름 색인, 학번키, 연도별 소속 충돌, 고아 팀원·보고서·상담·설문·프로그램·AI 기록을 검사한다.

## 2. 이번 점검에서 수정한 항목

1. `career-test.html`
   - 상용 검사·O*NET 제품명 노출 제거
   - 자체 개발 문항 안내로 변경
   - 진행 표시 42문항을 실제 72문항으로 수정
2. `consulting.html`
   - 모바일 2열 입력란을 1열로 전환
   - 입력란 화면 이탈과 작은 시간 선택 버튼 개선
3. `career-admin.html`
   - 모바일에서 900px 표가 상위 그리드를 밀어내던 문제 수정
   - 표 내부에서만 가로 스크롤하도록 제한
4. `report.html`
   - 2026/2025 고정 연도 제거
   - 현재 연도 기준 목록 자동 생성
   - 로그인한 동아리의 등록 연도로 고정하여 다른 연도 오등록 방지
   - 빈 접근코드 제출 시 오류 안내와 포커스 제공
5. `admin.html`
   - 동아리명·대표자명·팀원 정보 출력 시 HTML/인라인 스크립트 이스케이프 적용
   - 악성 입력 또는 따옴표로 인한 관리자 화면 손상/XSS 위험 완화
6. `survey-builder.html`
   - 로그인 여부뿐 아니라 등록된 관리자 이메일까지 확인하도록 강화
7. `netlify.toml`
   - 사용하지 않는 과거 Netlify Function 배포 해제
   - 새 Firebase 클라이언트 화면을 비밀검사 예외 목록에 추가해 `Exposed secrets detected` 재발 방지
8. `README.md`
   - GitHub/Claude/Gemini 서버리스 생성기 안내 제거
   - Firebase 대기열 + 로컬 Ollama 현재 구조로 정리

## 3. 파이프라인 호환성

### 창업동아리

등록 신청 → HWP 양식 다운로드 → 작성 HWP 제출 → 관리자 승인 → 동아리/대표자/학생원장/연도별 소속 생성 → 접근코드 발급 → 활동문서 제출 → AI 피드백 → 상담 → 관리자 파일·실적 관리

판정: 연결됨. 신청 승인 시 대표자 1명이 생성되며 전체 팀원은 관리자 일괄 엑셀 또는 팀원 추가 CRUD로 보완한다.

### 연도 운영

동아리 문서에 연도를 보관하고 관리자 모든 주요 탭이 선택 연도로 필터링된다. 종료 동아리는 학생용 보고서·설문·AI·사이트 접근에서 차단된다.

판정: 수정 후 호환됨. 다음 연도에는 공식 신청 HWP/PDF와 신청 화면의 연도 문구를 운영자가 교체해야 한다.

### 웹사이트 제작

창업/취업 선택 → 동아리 접근코드 → 문서/PDF·선택값 입력 → `website_jobs` → 로컬 AI 실행기 → 30 디자인 × 30 레이아웃 × 30 팔레트 × 30 타이포그래피 → 샌드박스 미리보기 → 초안/수정/확정/공개범위 관리

판정: 연결됨. 참고 사이트는 내부망·로컬 주소를 차단한 뒤 공개 HTML의 색상·구조 단서만 분석하며 원문 복제를 지시하지 않는다.

### 취업 진로

72문항 검사 또는 기존 결과지/검사 건너뛰기 → 결과 PDF 저장 안내 → 희망 직무 기준 선택 → 취업 포트폴리오 생성 → 관리자 익명 통계·요약 기록

판정: 연결됨. 검사 원문 문항은 자체 작성이며 결과는 표준화 심리검사나 채용 판정으로 표시하지 않는다.

## 4. CRUD 점검

| 영역 | Create | Read | Update | Delete | 무결성 보호 |
|---|---:|---:|---:|---:|---|
| 동아리 | 확인 | 확인 | 확인 | 확인 | 연도+이름 중복, 접근코드 해시 |
| 학생/팀원 | 확인 | 확인 | 확인 | 확인 | 학번 원장, 연도+학번 소속키, 팀장 최소 1명 |
| 신청서 | 확인 | 확인 | 승인/반려 | 업로드 실패 정리/관리자 삭제 | HWP·20MB·경로 검증 |
| 보고서/파일 | 확인 | 확인 | 분류·동아리·연도 이동 | 확인 | Firestore와 Storage 동시 이동 |
| 상담 | 확인 | 확인 | 상태 변경 | 확인 | 선택 동아리 활성 상태 확인 |
| 설문 | 확인 | 확인 | 응답수 원자 증가 | 확인 | 관리자 생성, 학생 응답 분리 |
| AI 기록 | 대기열 생성 | 확인 | 작업 상태/결과 | 관리자 | 트랜잭션 작업 선점 |
| 제작 사이트 | 생성 | 공개/코드 접근 | 수정본·상태·공개범위 | 직접 삭제 UI 없음 | 동아리 귀속, 샌드박스 iframe |

운영 데이터가 있는 프로덕션에서 임의 생성·삭제를 실행하지 않았으며, 쓰기 경로는 소스·규칙 계약과 빈 입력 검증으로 확인했다. 실제 배포 후에는 별도의 테스트 동아리 1개로 생성→팀원 추가/수정/삭제→파일 이동→종료 순서의 스모크 테스트를 권장한다.

## 5. 확인된 잔여 위험과 권장 우선순위

### P1 — 공개 동아리 문서 최소화

현재 학생이 접근코드로 로그인하기 위해 `clubs/{clubId}` 단건 읽기를 허용하며 이 문서에는 대표자 연락처·이메일이 함께 들어갈 수 있다. 추측 가능한 과거 문서 ID를 아는 사용자가 단건 조회할 가능성을 줄이려면 다음 버전에서 공개용 `club_access` 문서에 `clubId/name/year/status/field`만 저장하고 원본 `clubs` 읽기는 관리자 전용으로 변경하는 마이그레이션이 필요하다.

### P1 — 개인정보 보존기간/삭제 UI

진로 데이터, 신청서, 상담, 보고서의 보존기간을 정하고 관리자 화면에서 연도별 보관·삭제 기능과 삭제 전 내보내기를 제공하는 것이 좋다. 현재 진로 데이터는 Firebase Console에서 삭제 가능하지만 관리자 UI의 삭제 기능은 없다.

### P2 — 실운영 자동 테스트 환경

프로덕션 데이터와 분리된 Firebase 테스트 프로젝트 또는 Emulator Suite를 구성하면 동아리/학생/파일 CRUD를 매 배포마다 자동 검증할 수 있다. 현재는 프로덕션 훼손을 피하기 위해 파괴적 테스트를 자동 실행하지 않는다.

### P2 — 관리자 권한 중앙화

관리자 이메일 목록이 규칙과 여러 HTML에 중복되어 있다. 향후 Firebase Custom Claims 또는 관리자 컬렉션으로 중앙 관리하면 인사 변경 시 누락 위험이 줄어든다.

### P3 — 운영 편의 기능

- 연도 마감 전 전체 백업 ZIP/CSV 생성
- 제출 누락 동아리 자동 알림 예약
- 파일 분류 실패/중단 작업 재개
- 제작 사이트 관리자 삭제·복구함
- 진로 데이터 보존기간 일괄 삭제

## 6. 검증 결과

- HTML 내부 모듈 스크립트 문법: 오류 없음
- Netlify 함수/JavaScript 문법: 오류 없음
- Python 실행기/AI worker 컴파일: 오류 없음
- 로컬 웹사이트 렌더러: 창업·취업 샘플 모두 유효한 단일 HTML 생성
- 디자인 카탈로그: 디자인 30, 레이아웃 30, 팔레트 30, 타이포그래피 30
- 데스크톱 공개 화면: 가로 넘침 없음
- 모바일 공개 화면: 수정 전 발견한 상담·진로관리 넘침 수정
- 외부/로컬 파일 참조: 필수 신청 HWP/PDF 포함, 누락 없음
- 실제 배포 콘솔: 검사한 공개 화면에서 경고·오류 없음
- Firebase CLI 규칙 컴파일: 로컬 CLI 미설치로 이번 점검에서는 실행하지 못함. 현재 규칙은 기존 배포본과 정적 계약을 검토함.

## 7. 사용자 재점검 지적사항 반영

### 사업계획서 내용 미반영

- 원인: PDF에서 추출된 전문이 `추가 원문`으로만 저장되고 고정 렌더러의 공개 섹션에는 직접 연결되지 않았음.
- 수정: AI 실행기가 추가 원문을 사실 기반 JSON 항목으로 먼저 구조화하고, 사용자가 직접 쓴 값은 보존하면서 빈 값·`본문 참고` 항목만 보강함.
- 원문 전문은 공개 HTML에 그대로 노출하지 않으며, 생성 작업 완료 후 기존 정책대로 작업 문서에서 삭제함.

### 생성 사이트 디자인 품질·다양성

- 기존 30종 카탈로그의 일부 차이가 명칭·색·폰트 중심으로 느껴질 수 있었음.
- 30개 콘셉트를 10개 실제 시각 계열 × 3개 배열 변형으로 분리하고, 기존 30개 레이아웃·30개 팔레트·30개 타이포그래피와 결합함.
- 모바일에서는 강한 데스크톱 효과를 안전하게 해제해 가로 넘침과 읽기 불편을 방지함.
- 전체 HTML을 소형 LLM에 맡기지 않고, AI는 원문 구조화·참고 이미지/사이트 분석에 사용하고 반응형 렌더링은 검증된 디자인 엔진이 담당함.

### 취업 포트폴리오 HTML 다운로드

- 결과 HTML이 로드되기 전부터 상단에 `HTML 준비 중…` 버튼을 표시함.
- 로드가 끝나면 같은 위치에서 강조된 `HTML 다운로드` 버튼으로 즉시 전환되므로 버튼이 사라진 것으로 오해하지 않음.

### 진로검사 결과지 설명

- 화면에 상위 3개 유형 상세 해석과 점수·코드 읽는 법을 추가함.
- 낮은 점수는 능력 부족이나 부적합을 뜻하지 않는다는 해석 주의를 명시함.
- PDF를 A3 가로 2쪽으로 확장: 1쪽 시각 요약, 2쪽 유형별 상세 설명·점수 해석·탐색 행동.

### 재검증

- HTML 내부 스크립트 15개 문법 검사 통과.
- Python 실행기·worker·렌더러 컴파일 통과.
- 창업·취업 렌더러 테스트 통과.
- 최신 AI 실행기 EXE 재빌드 완료.

## 8. 배포 후 실제 생성 테스트

- 진로검사 72문항 완주: 결과코드·막대그래프·상세 해석·점수 안내·PDF 생성 상태 정상.
- 진로검사 → 취업 포트폴리오 이동: 검사 결과 기반 선택과 추천 방향 전달 정상.
- 취업 생성 테스트: `codex-career-audit-0812` 생성 완료, 동아리 코드 접근, HTML 다운로드 활성화, 모바일 가로 넘침 없음.
- 창업 생성 테스트: `codex-startup-audit-0812` 생성 완료, 원문의 `이중 검증 대시보드`, 월별 성장 변화, 기록 출처, 포트폴리오 내보내기가 결과에 반영됨.
- 실사용 점검에서 `본문 파일 참고`처럼 두 단어 이상으로 된 자리표시자가 그대로 남는 경우를 발견해 감지 규칙을 추가 보강함.
- 진로 결과의 한국어 조사 표현을 자연스럽게 수정함.
- 위 최종 보강을 포함해 AI 실행기 EXE를 다시 빌드함.
