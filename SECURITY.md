# 보안 운영 원칙

## 즉시 교체해야 하는 값

기존 이관 문서에 기록됐던 관리자 비밀번호와 API 토큰은 노출된 값으로
간주합니다. 재사용하지 말고 새 값으로 교체해야 합니다.

## 권한 구조

- 관리자: Firestore 규칙의 허용 이메일과 일치하는 Firebase Authentication 사용자
- 공개 사용자: 동아리 등록, 상담 신청, 설문 응답
- 동아리 사용자: 동아리 접근코드를 알고 있는 사용자
- AI 처리: Firebase 작업 대기열을 서비스 계정으로 읽는 로컬 실행기

동아리 접근코드는 완전한 사용자 인증이 아닙니다. 현재 기능을 유지하기 위해
코드 기반 조회와 제출을 허용하지만, 코드를 외부에 공개해서는 안 됩니다.

## 배포 전 확인

- Firestore와 Storage 규칙을 실제 프로젝트에 배포했는지 확인
- Firebase App Check 적용 검토
- Netlify에 비밀 환경변수가 등록됐는지 확인
- 저장소와 이관 문서에 평문 비밀번호가 없는지 확인
- Firebase 서비스 계정 JSON이 GitHub와 웹 배포 폴더에 없는지 확인
- Google Cloud 결제 예산 알림 설정

## 남은 장기 개선

기능을 바꾸지 않는 이번 정비 범위에서는 동아리 코드 로그인을 유지했습니다.
다음 단계에서는 동아리별 Firebase 계정 또는 일회용 로그인 링크로 전환하면
보고서와 파일을 사용자 단위로 더 강하게 보호할 수 있습니다.
